# Caffeine Deployment Log — Anti Krisis (fork)

One row per Caffeine preview push. Append-only; newest at the bottom.
Format: version · bundle hash · commit · what changed · verification state · rollback note.

Project ID: 019e2022-fb01-744c-b2b7-d8ed5b4d430d
Draft: https://yodelling-tan-nev-draft.caffeine.xyz · Live: https://anti-krisis-beta-tjv.caffeine.xyz

| Ver | Date | Bundle (draft hash) | Commit(s) | What went in | Verified by |
|-----|------|--------------------|-----------|--------------|-------------|
| 264 | 08-26 | (pre-AKK era) | `55c6221` — last v264 export commit; live prod since | mops+tsc+build; live | user (live) |
| 265 | 08-26 | `index-Dmh6IjXw.js` | AKK-2 CAS guard + root-`frontend/` removal | draft hash match ✓ | — |
| 270 | 08-27 | `index-B5LYNadr.js` | AKK-1b endpoint removal (self-send NOT included) | draft hash `B5LYNadr` ✓ → user promoted live | user |
| 277 | 08-27 | — | (no CLI push — composer-side builds) | user test | user |
| 278 | 08-27 | `index-DfFPdD7A.js` | + AKK-4 v1 binding-in-fee-tx | draft hash ✓ — wallet rejected fee tx (internal-account calldata block) | user test FAILED |
| 279 | 08-27 | `index-B0Llj0tg.js` | + error-surfacing fixes (`0674712`); still fee-embedded binding → wallet-rejected | user test hit hang, error surfaced correctly | user test FAILED (root cause found) |
| — | — | `index-B6m0Q9ZR.js` | AKK-4b self-send design (`ce21506`) | built, never pushed — REJECTED by user; revert before Option B | n/a |
| — | 08-28 | — | **revert `ce21506` → `a816084`** on `security/akk4b-selfsend-binding`; src/ byte-identical to `0674712` (v279 base for Option B) | no push (local revert only) | git diff empty |
| 281 | 08-28 | `index-CWyECbSn.js` | **AKK-4 Option B** (`a816084`+`dc9ef35`+`5690465`): FeeCollector (DEPLOYED on 4 chains, `0x6cBB624D…74aCD2`, owner=EOA) + FeePaid-event check (DISARMED) + admin UI section + migration `20260828_000000` (stable-compat ✓). Behavior identical to v279 until feeRecipient switched to collector. (Caffeine feed confirms "Draft Version 281 deployed and awaiting review"; two 502/504 attempts before it landed) | draft hash match ✓ — user testing burn→fee→claim on draft | all gates green; FeePaid parser 8/8 |
| — | 08-28 | — | draft backend (`cfi3s-…`) config flipped via akk-deployer CLI: feeRecipient → collector, collectorAddress → collector, **FeePaid check ARMED**. Draft now runs the FULL Option B path (fee→collector + event proof). User's v281 pre-flip test failed at fee step as predicted (recipient was still EOA) | readbacks: `(opt "0x6cBB…")`, `(true)`, `("0x6cBB…")` ✓ | — |
| 283 | 08-28 | `index-CWyECbSn.js` (unchanged — backend-only fix) | **fix `aab30d1`: FeePaid topic0 corrected** to the real 3-param signature `0x63067056…daa027` (was phantom 4-param `0x35f4df2d…` — parser could never match live events). Found via user's on-chain-confirmed fee `0xd1536a6c…` that the app ignored; parser re-proven against the REAL receipt (recognizes it, rejects wrong payer/collector) | **SELF-HEAL PROVEN**: stuck claim `0x4a31ef2b…` flipped pendingFee→verified, gritMinted `1_252_000_000`, paid via the stored fee `0x00a150b1…` (FeePaid matched) — no re-payment needed. Extra fee `0xd1536a6c…` (~$0.01) sits in the collector, recoverable via owner sweep | status=verified ✓ via CLI readback; several 502/504s before landing (server-side registration worked anyway) | rollback: disarm check + revert feeRecipient to EOA (both instant, CLI/admin) |
| 284 | 08-28 | `index-DJ1fVZH4.js` → `index-BwCS1W2t.js` | **kVCM burn reliability** (`b30b4f9`+`e8b7b78`): ① retire-tx gas capped at 20M (Base rejected 140M wallet estimates — `exceeds maximum per-tx gas limit`), ② price-deviation no longer FAILS the claim (stays pending, auto-repriced; AKK-3 fail-closed unchanged), ③ failed-claim resurrection: re-claiming a failed burn revives it in place (original claimant only, no duplicate record), ④ **Retry Claim** button on failed rows reuses the already-bound fee (no new payment), ⑤ "on Ethereum" wording made chain-neutral. Landing registered server-side despite 504s; bundle `BwCS1W2t` is the shippable | draft hash `BwCS1W2t` ✓ | gates all green; stranded claim `0xff08514c…` recoverable via new Retry Claim | rollback: revert commits; resurrect+retry paths are additive |
| 285 | 08-28 | `index-DiFn0qAR.js` | **Oracle pair-parsing + dropped-burn guard + approve wording** (`411ce04`): DexScreener parser rewritten — segments per pair ("baseToken" markers), only pairs where the queried token is the BASE token, highest-liquidity wins; validated on live payload (kVCM → $0.01391 exact; the $1.91-liq dust pair quoting IN kVCM that 11×-over-credited claim `0x8b061b…` is now structurally excluded). Burn modal polls public RPCs for the burn receipt; a tx unknown to ALL nodes after 90s fails honestly ("likely dropped — no platform fee charged") instead of spinning on CONFIRMING and prompting a fee. Approve-permission sentence shown in the modal before the wallet popup (MetaMask's own dialog text is not dapp-editable). Old mis-priced claims left as-is per user decision | draft hash `DiFn0qAR` ✓ (user: "v291 landed") | gates green; FeePaid-check config intact post-upgrade | revert commits; parser change is backend-only |
| 286 | 08-28 | `index-CAqUSPDA.js` | **Dead-RPC guard** (`2a2496a`): claim `0xe615f0cb…` (0.2 kVCM) failed with ALL on-chain facts correct (burn status 1 + 8 logs, fee→collector + FeePaid, sender match, binding payload exact) — killer was `base.llamarpc.com` dead (521 plaintext) accepted by `fetchTxByHash` as a valid response, then "tx missing from" → definitive BINDING_FAIL. Fix: JSON-RPC-shape guard (response must carry `"jsonrpc"`/`"result"` marker), llamarpc removed from backend Base set AND frontend viem client, `waitTxIndexed` switched to getTransactionByHash (mempool-aware). ORACLE FIX VALIDATED IN PRODUCTION: that claim's usdValue = 0.00278 = 0.2 × $0.01391 exactly | draft hash `CAqUSPDA` ✓ | gates green | revert commits |
| 287 | 08-28 | `index-DXxp5CN0.js` | **Route simulation before signing** (`48234e3`): decisive user evidence — wallet history shows approve mined but NO retire tx and NO kVCM deducted; the wallet returned a hash for a tx that never landed (embedded/AppKit relays bundle txs, can fail one post-hash; heavy Klima routes prime victims). Fix at the source: Phase A pre-approve — exact retire calldata eth_estimateGas'd per shuffled candidate; viable + allowance-blocked (0xfb8f41b2) routes shortlisted (≤6), all other reverts skipped before ANY wallet popup. One approve (allowance is kVCM→AAM, covers all candidates). Phase B post-approve — shortlist re-simulated with allowance live; first clean route wins with measured gas (1.3× headroom, ≤20M cap). Confirms ~90s modal note retained | draft hash `DXxp5CN0` ✓ | gates green (frontend-only) | revert commits |
| 288 | 08-28 | `index-CtJ_tXm7.js` | **Whitelist + tiered selection + allowance-aware gate** (`f8c3a83`+`a0aa3ec`): 9-credit whitelist from the full scan (19 eligible → 13 executable → ceilings laddered: 4×T1 12k–20k t, 2×T2 4k–8k t, 3×T3 1k–2k t, 3 dust, 6 dead). Tier by burn USD: ≤$6.9→all 9, ≤$69→T1+T2, above→T1 (shuffled within tier). Happy path = ZERO subgraph calls (rate-limited Goldsky); discovery is fallback-only. Allowance read once — insufficient allowance skips pre-approve sims (aggregator swallows the token's allowance revert, which made every healthy route read DEAD → the "(19 routes simulated)" error). Approve popup skipped when leftover allowance covers the burn. Burn-phase route errors no longer prefixed "Fee step failed…"; failStep lights honest (✕ on the real failing step). Text → "Burning kVCM natively via Klima Protocol..." | draft hash `CtJ_tXm7` ✓ | one full cycle already credited under v287 sim gate; tsc/biome/vite green | revert commits; whitelist is a code constant (Phase 2: backend state + admin editor) |
| 289 | 08-28 | `index-_eplQPu4.js` | **STILL_PROCESSING contract** (`28bab7b`): price-deviation claims are intentionally left #pending for auto-reprice, but the modal showed "Transaction failed" with ✕ on step 3 while the backend was seconds from crediting. Backend now returns the stable token STILL_PROCESSING (no jargon leak); the modal treats FEE_PENDING and STILL_PROCESSING as in-flight — step-3 spinner, "Processing GRIT credit…", polls until settlement; only definitive backend failures flip to failed. pending_verification line renamed | draft hash `_eplQPu4` ✓ | gates green (backend + frontend) | revert commits |
| 290 | 08-28 | `index-BSFfasIj.js` | **Modal messaging cleanup** (`f9b03bf`): failed-state footer note phase-accurate via failStep (burn → "Nothing was charged — try again"; fee → "Retry Fee resubmits the platform fee"; GRIT → "Retry Claim re-verifies at no extra cost") — matches the actual Burn History buttons. `pending_fee` line renamed "Fee payment needs a retry." (it was claiming "under a minute" for a failed fee); "Waiting for RPC indexing…" → "Confirming on-chain…" | draft hash `BSFfasIj` ✓ | user confirms burn flow "working pretty smoothly" | revert commit |
| 291 | 08-28 | `index-KOTqbJot.js` | **pendingFee grace window** (`58150c6`): user screenshot — fee merely slow, modal showed step-2 "Fee payment needs a retry." + banner "Platform fee failed on-chain — please retry the fee payment." then the fee settled and GRIT credited untouched. Both poll loops treated backend `#pendingFee` as definitive fee failure and STOPPED polling; pendingFee really means "burn verified, fee not yet credited" (~15s backend recheck in the healthy case). Now: 90s grace in step-2 waiting state (spinner + "credits automatically"), polling continues, escalation wording honest ("if it was actually paid, Retry Fee resolves it without a new payment"). Grace ref reset on handleReset | draft hash `KOTqbJot` ✓ | tsc/biome/vite green | revert commit |
| 292 | 08-28 | `index-p1EzGF8j.js` | **Celo binding-fail diagnosis + wording pass** (`21e40e5`): failed axlREGEN/Celo claim `0x3b4613…` — recorded fee `0x4da7d0e4…` is result:null on ALL four Celo RPCs; the wallet relay accepted-then-dropped the fee tx (same class as Base retire drops). Binding check behaved CORRECTLY (refused a payment it couldn't see); recovery = Burn History → Retry Fee. Wording per user: "Still awaiting confirmation… monitor Burn History for status." + "Submitting claim…" | draft hash `p1EzGF8j` ✓ | gates green | revert commit |
| 293 | 08-28 | `index-c3fr4nIk.js` | **Retry-claim honesty + STILL_PROCESSING as submitted** (`f39c0da`): IMPT/ETH claim `0x328495…` had both txs perfect on-chain (verified by full pipeline replica against real RPC data — every check passes) yet stayed #failed while Burn History showed "✓ Re-submitted" (the ✕-retry threw and the catch hid the failure marker). Fix: retry errors now visibly ✕-marked with the real reason; useInitiateClaim treats STILL_PROCESSING like PENDING (submitted → poller watches). NOTE: the Celo twin `0x3b4613…` self-healed to verified 1_411_520_000 via retry — the two claims had shared one fee hash (first failed pre-recipient-flip, retry bound to live config) | draft hash `c3fr4nIk` ✓ | gates green; next Retry Claim click on the IMPT row settles it or shows the true reason | revert commit |
| 294 | 08-28 | `index-c3fr4nIk.js` (UNCHANGED — backend-only) | **No-expiry recovery model + transient binding guard** (`79b1c10`), per user spec after the three phantom-failed claims: ① claims NEVER expire/delete (1h purge is a no-op; failed rows stay retryable forever), ② no 35-min age-out to #failed on unreachable verification (stays #pending, self-heals), ③ **root cause closed**: recheckFeeClaim treated "tx missing `from`" (transient RPC artifact) as DEFINITIVE binding failure — healthy claims with perfect on-chain data were failed by one bad RPC response. Guard: only structural mismatches (recipient/sender/calldata) fail a claim; missing-field/not-found errors keep it pending. Probe evidence: all 3 claims' burns status=1, fees → collector + FeePaid + exact bindings (Celo fees confirmed Celo-chain incl. `0xa223572e` per user's CeloScan link) | bundle hash unchanged (backend-only) — verify by behavior: Retry Claim now settles or shows the true ✕ reason | mops check + stable-compat ✓, mops build ✓ | revert commit |
| 295 | 08-28 | `index-BhVR8yTp.js` | **Burn-outcome modal gate + Pay Fee button** (`f3cf2b3`): burn-verification lifecycle per user spec. waitBurnReceipt polls for the burn RECEIPT (mining + status): reverted → "Burn Failed" terminal, no fee (old flow could charge a fee for a reverted burn); unsettled after 90s → claim submitted WITHOUT fee + "continuing in background. Monitor Burn History" (old "dropped" verdict could double-burn a slow tx that later mined); settled+success → fee flow as before. Burn History gains a **Pay Fee** button for pending claims with no feeTxHash — burn is never stranded; user finishes from history | draft hash `BhVR8yTp` ✓ | gates green | revert commit |
| 296 | 08-28 | `index-IP_1tsih.js` | **Deep-review fixes** (`d3cc7c2`): ① CRITICAL — slow-burn background path registered claimedTxRef after entering pending_verification, but the modal poller keys off claimedTxRef → poller never started, claim had no live monitoring; now registered before submission. ② Pay Fee had no backend support — retryFeeClaim required #pendingFee but background claims sit in #pending (no fee yet); now accepts #pendingFee OR #pending-with-empty-fee (mid-verification claims redirected to Retry Claim). ③ "Burn Failed" text shortened per user | draft hash `IP_1tsih` ✓ | all gates green incl. stable-compat | revert commit |
| — | 08-29 | — | **DRAFT BACKEND RESET BY CAFFEINE** (fresh-state behavior): env.json now points to NEW canister `ke4xm-pqaaa-aaaah-qqvsq-cai` (old `cfi3s-…` is no longer our program — all draft config + test claim history gone with it). New canister runs latest code (Option B methods present) with defaults. Config re-applied via akk-deployer CLI: recipient → collector, collector set, FeePaid check armed — readbacks verified. **LESSON: any post-reset draft/live needs the 3-set config re-run; live (`g4i7o-…`) still on OLD build (has getFeeRecipient, no Option B methods) with recipient already = collector (plain transfers, sweepable)** | readbacks `(opt "0x6cBB…")`, `("0x6cBB…")`, `(true)` ✓ | — | — |

## Version → commit reconstruction rules

- Caffeine versions increment per successful upload; composer-side pushes may skip numbers.
- Ground truth = draft URL's `index-*.js` hash vs local `pnpm build` output hash.
- v277 = GitHub main `06d5908` (squash-merged export by Caffeine; our commits AKK-2/1b/3/UX
  arrived there as squash commits `55c6221`+`06d5908`).
- Live URL strips `<script>` tags via CDN transform — bundle-hash check ONLY works on the
  draft domain.

## Rollback procedure (tested path)

1. Identify target: live = v277 (`06d5908` on GitHub main).
2. Build locally from that commit, `caffeine preview --build --project . --json` → draft N+1.
3. Verify draft hash, user reviews, promotes draft → live from Caffeine dashboard.
4. GitHub sync happens only after user verifies (their flow).

## After Option B lands, expected entries

- v280: FeeCollector contract + reverted self-send + FeePaid-event check (AKK-4 final form)
- Phase 2: aggregator consolidation + ckETH → ICPSwap → notify_top_up + watchdog
