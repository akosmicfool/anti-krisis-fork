# Caffeine Deployment Log — Anti Krisis (fork)

One row per Caffeine preview push. Append-only; newest at the bottom.
Format: version · bundle hash · commit · what changed · verification state · rollback note.

Project ID: 019e2022-fb01-744c-b2b7-d8ed5b4d430d
Draft: https://yodelling-tan-nev-draft.caffeine.xyz · Live: https://anti-krisis-beta-tjv.caffeine.xyz

| Ver | Date | Bundle (draft hash) | Commit(s) | What went in | Verified by |
|-----|------|--------------------|-----------|--------------|-------------|
| 264 | 08-26 | (pre-AKK era) | `55c6221` — last v264 export commit; live prod since | mops+tsc+build; live | user (live) |
| 265 | 08-26 | `index-Dmh6IjXw.js` | AKK-2 CAS guard + root-`frontend/` removal | draft hash match ✓ | — |
| 270 | 08-27 | `index-B5LYNadr.js` | AKK-1b endpoint removal (self-send NOT included) | draft hash `B5LYNadr` ✓ → user promoted live | user |
| 277 | 08-27 | — | (no CLI push — composer-side builds) | user test | user |
| 278 | 08-27 | `index-DfFPdD7A.js` | + AKK-4 v1 binding-in-fee-tx | draft hash ✓ — wallet rejected fee tx (internal-account calldata block) | user test FAILED |
| 279 | 08-27 | `index-B0Llj0tg.js` | + error-surfacing fixes (`0674712`); still fee-embedded binding → wallet-rejected | user test hit hang, error surfaced correctly | user test FAILED (root cause found) |
| — | — | `index-B6m0Q9ZR.js` | AKK-4b self-send design (`ce21506`) | built, never pushed — REJECTED by user; revert before Option B | n/a |
| — | 08-28 | — | **revert `ce21506` → `a816084`** on `security/akk4b-selfsend-binding`; src/ byte-identical to `0674712` (v279 base for Option B) | no push (local revert only) | git diff empty |
| 281 | 08-28 | `index-CWyECbSn.js` | **AKK-4 Option B** (`a816084`+`dc9ef35`+`5690465`): FeeCollector (DEPLOYED on 4 chains, `0x6cBB624D…74aCD2`, owner=EOA) + FeePaid-event check (DISARMED) + admin UI section + migration `20260828_000000` (stable-compat ✓). Behavior identical to v279 until feeRecipient switched to collector. (Caffeine feed confirms "Draft Version 281 deployed and awaiting review"; two 502/504 attempts before it landed) | draft hash match ✓ — user testing burn→fee→claim on draft | all gates green; FeePaid parser 8/8 |
| — | 08-28 | — | draft backend (`cfi3s-…`) config flipped via akk-deployer CLI: feeRecipient → collector, collectorAddress → collector, **FeePaid check ARMED**. Draft now runs the FULL Option B path (fee→collector + event proof). User's v281 pre-flip test failed at fee step as predicted (recipient was still EOA) | readbacks: `(opt "0x6cBB…")`, `(true)`, `("0x6cBB…")` ✓ | — |
| 283 | 08-28 | `index-CWyECbSn.js` (unchanged — backend-only fix) | **fix `aab30d1`: FeePaid topic0 corrected** to the real 3-param signature `0x63067056…daa027` (was phantom 4-param `0x35f4df2d…` — parser could never match live events). Found via user's on-chain-confirmed fee `0xd1536a6c…` that the app ignored; parser re-proven against the REAL receipt (recognizes it, rejects wrong payer/collector) | **SELF-HEAL PROVEN**: stuck claim `0x4a31ef2b…` flipped pendingFee→verified, gritMinted `1_252_000_000`, paid via the stored fee `0x00a150b1…` (FeePaid matched) — no re-payment needed. Extra fee `0xd1536a6c…` (~$0.01) sits in the collector, recoverable via owner sweep | status=verified ✓ via CLI readback; several 502/504s before landing (server-side registration worked anyway) | rollback: disarm check + revert feeRecipient to EOA (both instant, CLI/admin) |
| 284 | 08-28 | `index-DJ1fVZH4.js` → `index-BwCS1W2t.js` | **kVCM burn reliability** (`b30b4f9`+`e8b7b78`): ① retire-tx gas capped at 20M (Base rejected 140M wallet estimates — `exceeds maximum per-tx gas limit`), ② price-deviation no longer FAILS the claim (stays pending, auto-repriced; AKK-3 fail-closed unchanged), ③ failed-claim resurrection: re-claiming a failed burn revives it in place (original claimant only, no duplicate record), ④ **Retry Claim** button on failed rows reuses the already-bound fee (no new payment), ⑤ "on Ethereum" wording made chain-neutral. Landing registered server-side despite 504s; bundle `BwCS1W2t` is the shippable | draft hash `BwCS1W2t` ✓ | gates all green; stranded claim `0xff08514c…` recoverable via new Retry Claim | rollback: revert commits; resurrect+retry paths are additive |
| 285 | 08-28 | `index-DiFn0qAR.js` | **Oracle pair-parsing + dropped-burn guard + approve wording** (`411ce04`): DexScreener parser rewritten — segments per pair ("baseToken" markers), only pairs where the queried token is the BASE token, highest-liquidity wins; validated on live payload (kVCM → $0.01391 exact; the $1.91-liq dust pair quoting IN kVCM that 11×-over-credited claim `0x8b061b…` is now structurally excluded). Burn modal polls public RPCs for the burn receipt; a tx unknown to ALL nodes after 90s fails honestly ("likely dropped — no platform fee charged") instead of spinning on CONFIRMING and prompting a fee. Approve-permission sentence shown in the modal before the wallet popup (MetaMask's own dialog text is not dapp-editable). Old mis-priced claims left as-is per user decision | draft hash `DiFn0qAR` ✓ (user: "v291 landed") | gates green; FeePaid-check config intact post-upgrade | revert commits; parser change is backend-only |
| 286 | 08-28 | `index-CAqUSPDA.js` | **Dead-RPC guard** (`2a2496a`): claim `0xe615f0cb…` (0.2 kVCM) failed with ALL on-chain facts correct (burn status 1 + 8 logs, fee→collector + FeePaid, sender match, binding payload exact) — killer was `base.llamarpc.com` dead (521 plaintext) accepted by `fetchTxByHash` as a valid response, then "tx missing from" → definitive BINDING_FAIL. Fix: JSON-RPC-shape guard (response must carry `"jsonrpc"`/`"result"` marker), llamarpc removed from backend Base set AND frontend viem client, `waitTxIndexed` switched to getTransactionByHash (mempool-aware). ORACLE FIX VALIDATED IN PRODUCTION: that claim's usdValue = 0.00278 = 0.2 × $0.01391 exactly | draft hash `CAqUSPDA` ✓ | gates green | revert commits |
| 287 | 08-28 | `index-DXxp5CN0.js` | **Route simulation before signing** (`48234e3`): decisive user evidence — wallet history shows approve mined but NO retire tx and NO kVCM deducted; the wallet returned a hash for a tx that never landed (embedded/AppKit relays bundle txs, can fail one post-hash; heavy Klima routes prime victims). Fix at the source: Phase A pre-approve — exact retire calldata eth_estimateGas'd per shuffled candidate; viable + allowance-blocked (0xfb8f41b2) routes shortlisted (≤6), all other reverts skipped before ANY wallet popup. One approve (allowance is kVCM→AAM, covers all candidates). Phase B post-approve — shortlist re-simulated with allowance live; first clean route wins with measured gas (1.3× headroom, ≤20M cap). Confirms ~90s modal note retained | draft hash `DXxp5CN0` ✓ | gates green (frontend-only) | revert commits |
| 288 | 08-28 | `index-CtJ_tXm7.js` | **Whitelist + tiered selection + allowance-aware gate** (`f8c3a83`+`a0aa3ec`): 9-credit whitelist from the full scan (19 eligible → 13 executable → ceilings laddered: 4×T1 12k–20k t, 2×T2 4k–8k t, 3×T3 1k–2k t, 3 dust, 6 dead). Tier by burn USD: ≤$6.9→all 9, ≤$69→T1+T2, above→T1 (shuffled within tier). Happy path = ZERO subgraph calls (rate-limited Goldsky); discovery is fallback-only. Allowance read once — insufficient allowance skips pre-approve sims (aggregator swallows the token's allowance revert, which made every healthy route read DEAD → the "(19 routes simulated)" error). Approve popup skipped when leftover allowance covers the burn. Burn-phase route errors no longer prefixed "Fee step failed…"; failStep lights honest (✕ on the real failing step). Text → "Burning kVCM natively via Klima Protocol..." | draft hash `CtJ_tXm7` ✓ | one full cycle already credited under v287 sim gate; tsc/biome/vite green | revert commits; whitelist is a code constant (Phase 2: backend state + admin editor) |
| 289 | 08-28 | `index-_eplQPu4.js` | **STILL_PROCESSING contract** (`28bab7b`): price-deviation claims are intentionally left #pending for auto-reprice, but the modal showed "Transaction failed" with ✕ on step 3 while the backend was seconds from crediting. Backend now returns the stable token STILL_PROCESSING (no jargon leak); the modal treats FEE_PENDING and STILL_PROCESSING as in-flight — step-3 spinner, "Processing GRIT credit…", polls until settlement; only definitive backend failures flip to failed. pending_verification line renamed | draft hash `_eplQPu4` ✓ | gates green (backend + frontend) | revert commits |
| 290 | 08-28 | `index-BSFfasIj.js` | **Modal messaging cleanup** (`f9b03bf`): failed-state footer note phase-accurate via failStep (burn → "Nothing was charged — try again"; fee → "Retry Fee resubmits the platform fee"; GRIT → "Retry Claim re-verifies at no extra cost") — matches the actual Burn History buttons. `pending_fee` line renamed "Fee payment needs a retry." (it was claiming "under a minute" for a failed fee); "Waiting for RPC indexing…" → "Confirming on-chain…" | draft hash `BSFfasIj` ✓ | user confirms burn flow "working pretty smoothly" | revert commit |
| 291 | 08-28 | `index-KOTqbJot.js` | **pendingFee grace window** (`58150c6`): user screenshot — fee merely slow, modal showed step-2 "Fee payment needs a retry." + banner "Platform fee failed on-chain — please retry the fee payment." then the fee settled and GRIT credited untouched. Both poll loops treated backend `#pendingFee` as definitive fee failure and STOPPED polling; pendingFee really means "burn verified, fee not yet credited" (~15s backend recheck in the healthy case). Now: 90s grace in step-2 waiting state (spinner + "credits automatically"), polling continues, escalation wording honest ("if it was actually paid, Retry Fee resolves it without a new payment"). Grace ref reset on handleReset | draft hash `KOTqbJot` ✓ | tsc/biome/vite green | revert commit |
| 292 | 08-28 | `index-p1EzGF8j.js` | **Celo binding-fail diagnosis + wording pass** (`21e40e5`): failed axlREGEN/Celo claim `0x3b4613…` — recorded fee `0x4da7d0e4…` is result:null on ALL four Celo RPCs; the wallet relay accepted-then-dropped the fee tx (same class as Base retire drops). Binding check behaved CORRECTLY (refused a payment it couldn't see); recovery = Burn History → Retry Fee. Wording per user: "Still awaiting confirmation… monitor Burn History for status." + "Submitting claim…" | draft hash `p1EzGF8j` ✓ | gates green | revert commit |
| 293 | 08-28 | `index-c3fr4nIk.js` | **Retry-claim honesty + STILL_PROCESSING as submitted** (`f39c0da`): IMPT/ETH claim `0x328495…` had both txs perfect on-chain (verified by full pipeline replica against real RPC data — every check passes) yet stayed #failed while Burn History showed "✓ Re-submitted" (the ✕-retry threw and the catch hid the failure marker). Fix: retry errors now visibly ✕-marked with the real reason; useInitiateClaim treats STILL_PROCESSING like PENDING (submitted → poller watches). NOTE: the Celo twin `0x3b4613…` self-healed to verified 1_411_520_000 via retry — the two claims had shared one fee hash (first failed pre-recipient-flip, retry bound to live config) | draft hash `c3fr4nIk` ✓ | gates green; next Retry Claim click on the IMPT row settles it or shows the true reason | revert commit |
| 294 | 08-28 | `index-c3fr4nIk.js` (UNCHANGED — backend-only) | **No-expiry recovery model + transient binding guard** (`79b1c10`), per user spec after the three phantom-failed claims: ① claims NEVER expire/delete (1h purge is a no-op; failed rows stay retryable forever), ② no 35-min age-out to #failed on unreachable verification (stays #pending, self-heals), ③ **root cause closed**: recheckFeeClaim treated "tx missing `from`" (transient RPC artifact) as DEFINITIVE binding failure — healthy claims with perfect on-chain data were failed by one bad RPC response. Guard: only structural mismatches (recipient/sender/calldata) fail a claim; missing-field/not-found errors keep it pending. Probe evidence: all 3 claims' burns status=1, fees → collector + FeePaid + exact bindings (Celo fees confirmed Celo-chain incl. `0xa223572e` per user's CeloScan link) | bundle hash unchanged (backend-only) — verify by behavior: Retry Claim now settles or shows the true ✕ reason | mops check + stable-compat ✓, mops build ✓ | revert commit |
| 295 | 08-28 | `index-BhVR8yTp.js` | **Burn-outcome modal gate + Pay Fee button** (`f3cf2b3`): burn-verification lifecycle per user spec. waitBurnReceipt polls for the burn RECEIPT (mining + status): reverted → "Burn Failed" terminal, no fee (old flow could charge a fee for a reverted burn); unsettled after 90s → claim submitted WITHOUT fee + "continuing in background. Monitor Burn History" (old "dropped" verdict could double-burn a slow tx that later mined); settled+success → fee flow as before. Burn History gains a **Pay Fee** button for pending claims with no feeTxHash — burn is never stranded; user finishes from history | draft hash `BhVR8yTp` ✓ | gates green | revert commit |
| 296 | 08-28 | `index-IP_1tsih.js` | **Deep-review fixes** (`d3cc7c2`): ① CRITICAL — slow-burn background path registered claimedTxRef after entering pending_verification, but the modal poller keys off claimedTxRef → poller never started, claim had no live monitoring; now registered before submission. ② Pay Fee had no backend support — retryFeeClaim required #pendingFee but background claims sit in #pending (no fee yet); now accepts #pendingFee OR #pending-with-empty-fee (mid-verification claims redirected to Retry Claim). ③ "Burn Failed" text shortened per user | draft hash `IP_1tsih` ✓ | all gates green incl. stable-compat | revert commit |
| 297 | 08-29 | `index--4TiUmLq.js` | **Celo empty-calldata root fix** (`5b4fa6c`): the recurring axlREGEN/Celo failures (claim `0xe7b3ff…` — fee to collector, FeePaid correct, 96-byte binding decoded byte-perfect from direct probe, yet "BINDING_FAIL: fee tx carries no binding payload") traced to `fetchTxByHash` accepting a result-bearing response with NO `input` field (truncated/partial object from flaky endpoints — 2 of 5 Celo RPCs broken: drpc -32601, meowrpc empty). Fix: empty-calldata responses signal PENDING (retry next endpoint/cycle) instead of feeding empty TxByHash into verification; "carries no binding payload" added to the transient set in all THREE binding-failure sites (recheckFeeClaim, recheckClaim, initiateClaim — transient keeps claim pending/retryable, structural mismatches still fail). Celo explorer link fixed in Burn History (was Etherscan) | draft hash `-4TiUmLq` ✓ | gates green; Retry Claim on the axlREGEN row should now settle | revert commit |
| — | 08-29 | — | **v317/v318 validation on reset draft backend (ke4xm)**: user tested axlREGEN/Celo full flow — **went through end-to-end** (empty-calldata root fix confirmed working). 8 of 9 claims verified across 4 chains with correct pricing. One straggler: `0xe7b3ff…` (the pre-fix binding-fail victim) sits #pendingFee — recovery paths available (background recheck or one Retry Fee click) | user: "I tested the axlREGEN flow and it went through this time" | readbacks ✓ | — |
| — | 08-29 | — | **DRAFT BACKEND RESET BY CAFFEINE** (fresh-state behavior): env.json now points to NEW canister `ke4xm-pqaaa-aaaah-qqvsq-cai` (old `cfi3s-…` is no longer our program — all draft config + test claim history gone with it). New canister runs latest code (Option B methods present) with defaults. Config re-applied via akk-deployer CLI: recipient → collector, collector set, FeePaid check armed — readbacks verified. **LESSON: any post-reset draft/live needs the 3-set config re-run; live (`g4i7o-…`) still on OLD build (has getFeeRecipient, no Option B methods) with recipient already = collector (plain transfers, sweepable)** | readbacks `(opt "0x6cBB…")`, `("0x6cBB…")`, `(true)` ✓ | — | — |
| — | 08-29 | `index--4TiUmLq.js` (UNCHANGED from v297 — backend-only) | **AKK-5 block-cycle reentrancy fix + height CAS** (`841be4c`+`26cd964`, cherry-picked onto `security/akk4b-selfsend-binding` tip `1c0a35a` = v297 source): ① `transient` reentrancy guard around runBlockCycle — overlapping 690s timer fires (possible with pendingMints backlog) are no-ops instead of double-draining GRIT/voiding a winner/corrupting history; released on success AND in re-throwing catch; 4h staleness self-heal (`tryEnterStale`) so a wedged guard can never freeze mining; ② `processBlock` takes caller-captured `thisBlockId`; `buildBlockRecord` height-CAS-rejects (returns null, no trap) any block that doesn't extend the live counter (parent-linkage analog); ③ first Motoko tests in repo (`src/backend/test/`, 15 tests via `mops test`). **Draft backend was FRESH AGAIN** (`lcl4h-…`): 3-set config re-applied + readbacks verified | draft hash ✓ = v297 bundle; readbacks `(opt "0x6cBB…")`, `("0x6cBB…")`, `(true)` ✓; stable-compat ✓ (transient outside stable layout, no migration) | revert commits; guard/CAS are additive safety, zero behavior change in normal operation |
| **LIVE** | 08-29 | (live CDN strips script tags — bundle hash not verifiable from HTML; verified via method presence) | **PROMOTED TO LIVE + user smoke-tested: "Looking good so far!"** | live backend `g4i7o-…` now runs the FULL v297 build: Option B methods present, **recipient = collector, FeePaid check ARMED, collector address set** — all three readbacks verified post-promotion, no re-config needed (Caffeine preserved the canister's state through this promotion, unlike the draft reset). getAllClaimHistory is admin-gated on live (caller identity differs from draft — expected; claim history lives with users) | readbacks `(opt "0x6cBB…")`, `(true)`, `("0x6cBB…")` ✓ · getFeePercent 0.69 ✓ · user live smoke test ✓ | rollback: disarm check + revert recipient (instant, CLI/admin) |
| — | 08-29 | `index--4TiUmLq.js` (UNCHANGED — backend-only) | **AKK-10 fee-AMOUNT verification** (`6535a93` on `security/akk-vuln-queue`; draft v320, clean push no 502s): FeePaid event `value` now decoded and required to cover feePercent × usdValue with 700bps tolerance (mirrors frontend-price deviation gate). Comparator per path: claim verification + timer rechecks use STORED claim-time usdValue (fee was sized then); retryFeeClaim (new fee) uses CURRENT oracle value. Native price via WETH (eth/base/op); celo skipped (no verified ERC-20 CELO address yet). Missing/unparseable event data = PENDING (transient, per recovery model). Deep review pre-commit caught 3 comparator bugs (recheck-time vs claim-time vs current — all fixed before push). Backend canister `lcl4h-…` NOT reset this time; config survived upgrade, readbacks re-verified | draft hash `-4TiUmLq` ✓ (= v297, backend-only); readbacks `(opt "0x6cBB…")`, `("0x6cBB…")`, `(true)` ✓; 26 tests green; stable-compat ✓; no .did change | revert commits; amount check additive — skips silently when prerequisites absent |
| — | 09-03 | `index--4TiUmLq.js` (unchanged — backend-only) | **AKK-10 HOTFIX push (draft v321)** (`967b646`): v320's amount check rejected EVERY honest fee on armed chains — two bugs: ① decoder read the LAST data word (binding-tail bytes ~1e77) instead of ABI head word 1 (`[0x40, value, …]`); ② minimumFeeWei converted USD→wei without dividing by the native price (~2400× overstated). Root-caused via replica against the 4 real receipts; all PASS with corrected math except kVCM (genuinely ~30% underpaid → Retry Fee). RPC sets re-probed (ankr API-gated, base.publicnode 403, meowrpc DNS-dead dropped; OP gained 2nd endpoint). | draft v321 hash `-4TiUmLq` ✓; 32 tests green; stable-compat ✓ | revert commits |
| — | 09-03 | `index--4TiUmLq.js` (unchanged — backend-only) | **DRAFT BACKEND RESET AGAIN** (`lcl4h-…` → fresh `xrshw-ziaaa-aaaao-qlgya-cai`; 4th reset). Old claim records gone with the old canister — user's Retry attempts on the stuck claims failed with "claim not found" (expected, not a bug). 3-set config re-applied via akk-deployer CLI on the fresh canister: recipient → collector, collector set, FeePaid check ARMED — readbacks verified. Token allowlist auto-seeded (seedVersion 3: kVCM/axlREGEN/TGN on base, axlREGEN celo, GIV optimism, IMPT ethereum). AKK-10 hotfix (`967b646`: ABI head-word-1 decode + native-price division) is in this build — amount check armed on ALL 4 chains incl. celo (ERC-20 CELO `0x471EcE…a438`, oracle-verified $0.077) | readbacks `(opt "0x6cBB…")`, `("0x6cBB…")`, `(true)` ✓ · getTokens 6 entries ✓ · getFeePercent 0.69 ✓ | — | rollback: disarm + revert recipient (instant) |
| — | 09-03 | `index--4TiUmLq.js` (unchanged — backend-only) | **BINDING BUGFIX push (draft v322)** (`20af781`): user's fresh-burn tests failed on TGN/axlREGEN-Base/IMPT with interchangeable errors. TWO root causes proven against real receipts: ① `hexToBytes` used 0xFF as invalid-digit sentinel — 0xFF is a LEGITIMATE byte (~11% of hashes) → binding silently rejected as "no binding payload" (TGN's burn ends f886ff; kVCM/Celo had no ff byte → passed); ② embedded-wallet RELAYED fees (tx.to = forwarder 0xdb9b…47db3, input = relay calldata) failed the recipient check — collector still emits FeePaid with binding intact; now armed-event proof satisfies recipient + supplies binding bytes (payer re-validated vs tx.from; sender check never relaxed). Validated end-to-end: relayed TGN binding decodes post-fix (principal = claimant, burn = actual tx 0x89ca…f886ff). IMPT "HTTP outcall failed" = transient RPC, timer retries | draft v322 hash ✓; canister NOT reset; readbacks ✓ (recipient=collector, armed); 42 tests green; stable-compat ✓; no .did change | revert commits; disarmed path unchanged |
| — | 09-03 | `index-CoZMRVAx.js` (NEW — frontend UX fix) | **pendingFee UX fix (draft v323)** (`baee8c6`): user confirmed all tokens+chains credit GRIT after v322, but IMPT (Ethereum) showed a false "fee failed / Retry Fee" while the fee was still confirming on-chain. Two defects: ① grace window was chain-blind (90s, L2-tuned — Ethereum L1 RPCs index in minutes) → now 5min on chainId 1, 90s on L2s; ② escalation STOPPED the poller → fee settled after escalation but modal never flipped to "GRIT Minted!" — now escalates display only (step-2 spinner + neutral grey note, never red) and KEEPS POLLING until POLL_TIMEOUT_MS; ③ wording "pending_fee" = "still monitoring", not "needs a retry". Backend timer (15s self-heal) unaffected — this was pure frontend presentation | draft v323, hash `CoZMRVAx` ✓ on draft; canister NOT reset; FeePaid still armed ✓ | revert commit; grace is presentation-only |
| — | 09-09 | `index-DethrGRL.js` (NEW — frontend + interface change) | **W0+W1A+W1B security package (draft v324)** (`33f771e`…`022e6f2`, 7 commits): ① **W1A canonical burn identity** — all hash spellings (0x/0X/none, any case) fold to one canonical `0x`+lowercase identity at initiateClaim/retryFeeClaim/recheckClaimByHash; closes the Critical capitalization/prefix replay across AKK-2/4/8; claim status+GRIT+price+fee-hash now commit in ONE CAS-guarded write (stale-retry metadata corruption closed); ② **W1B admin hardening** — addAdmin rejects anonymous principal (both blob shapes; `fromText("aaaaa-aa").isAnonymous()==false` under moc 1.11.2, documented), EVM address validation (`0x`+40 hex → lowercase) on all fee setters, **atomic `applySecureFeeConfig`** validates recipient==collector and commits recipient+collector+arm all-or-nothing (arm-setter REMOVED from did; disarm-only endpoint added); ③ **fresh-install fix (mandatory before fresh prod reinstall)** — first migration takes `{}` (Init pattern) instead of legacy shape; plain install_code onto empty state no longer traps (was vendor-lock-in only); REHEARSED in PocketIC; ④ deep review caught 2 regressions pre-push: mixed-case fee BINDING comparison + mixed-case modal matching — both now case-insensitive both sides | draft v324, hash `DethrGRL` ✓ on draft; **backend RESET AGAIN (5th: xrshw-… → fresh `6vpy5-paaaa-aaaag-auo7q-cai`)** — old claims gone (expected); **NEW: config re-applied via the new atomic `applySecureFeeConfig` endpoint itself** (one call: recipient+collector+arm — replaces the old 3-call ritual); readbacks: recipient `(opt "0x66Cc…")` ✓ seeded by fresh-install logic, collector+armed via atomic op ✓, getFeePercent 0.69 ✓, getTokens 6 entries ✓, getAdmins = akk-deployer ✓ (bootstrap seeding works on fresh install — proves the Init migration + seeding end-to-end); 10 backend test files green; stable-compat ✓; did changed (removed setter + 2 new methods) + bindgen + tsc/biome/vite ✓ | revert commits; interface change — old draft bindings incompatible with new backend (and vice versa); disarm+revert recipient to roll back FeePaid |
| — | 09-09 | `index-DethrGRL.js` (unchanged — backend-only) | **FEE-ADDRESS SEED FIX (draft v325)** (`07a0b60`): user tested v324 on Base — kVCM AND axlREGEN both failed at fee step with "External transactions to internal accounts cannot include data" (fee tx carried calldata to 0x66Cc… which has NO code — verified eth_getCode=0x on all 4 chains). Root cause: fresh-install seeding wrote the pre-Option-B owner EOA instead of the FeeCollector CONTRACT (0x6cBB624D…aCD2, CREATE2, verified live on base/eth/op/celo, owner()=0x66Cc…), and my post-reset applySecureFeeConfig call propagated the seeded value (recipient==collector invariant validates consistency, not correctness of the pair). **Fixed in draft runtime out-of-band** (applySecureFeeConfig re-run with correct address — no repush needed for the fix), plus the SEED corrected in main.mo (recipient=collector contract + collectorAddress + arm seeded) and **pushed as v325** so future resets self-configure correctly. Readbacks post-v325: recipient=collector=0x6cbb…aCD2, armed ✓ | draft v325, hash unchanged ✓ (backend-only), canister 6vpy5 survived (upgrade not reset), readbacks ✓ | revert commits; runtime already fixed via atomic op |
| — | 09-09 | `index-DD13Ovfb.js` (NEW — frontend flow change) | **CLAIM-FIRST BURN FLOW (draft v326)** (`75d723a`): user reported Celo fee-send failure (nonce race — wallet signed fee at stale nonce 146 while 3 rapid burns consumed 146-148) AND the deeper gap it exposed: fee-before-claim ordering meant ANY fee-send failure orphaned the burn (on-chain but never in Burn History, nothing to retry). Flow reordered: burn → initiateClaim(feeTxHash='') — claim lands in Burn History immediately as #pending (same deferred-fee path the slow-burn flow uses) → fee → retryFeeClaim binds + credits. Fee-phase failures now route to pending_fee with an amber recoverable message ('burn saved in Burn History — use Pay Fee') instead of terminal red failed; covers user rejection + nonce race + RPC errors. User's 3 Celo axlREGEN burns (146-148) are pre-fix orphans — claimable manually per burn hash | draft v326, hash `DD13Ovfb` ✓ on draft; canister 6vpy5 survived; FeePaid armed ✓ | revert commit; backend unchanged |
| — | 09-09 | `index-DD13Ovfb.js` (unchanged — backend-only) | **DEFERRED-FEE RETURNS #ok (draft v328)** (`0f17067`): user tested v327 — claim appears in Burn History but wallet popup never fires, modal jumps straight to "confirm fee tx". Root cause: my v327 fix returned #err(FEE_PENDING) from initiateClaim for deferred-fee submissions; FEE_PENDING's contract is "a fee tx WAS SENT and is confirming" → useInitiateClaim throws → handleBurn catch matches FEE_PENDING → enters the fee-WATCHING poll loop and returns BEFORE sendPlatformFee ever runs. User never asked to approve anything. Fix: deferred-fee submission is a SUCCESS — return #ok; flow continues to the wallet popup; slow-burn flow unaffected (Pay Fee appears in history as before). Both frontend pollers confirmed already case-insensitive (022e6f2) | draft v328, hash unchanged ✓ (backend-only), canister 6vpy5 upgraded, readbacks ✓ (recipient=collector=0x6cbb…aCD2, armed, 0.69); 10 test files green; stable-compat ✓ | revert commit |
| — | 09-09 | `index-CyHrQcYs.js` (NEW — frontend + backend) | **BUG-1 FIX: slow-verified fees stored, never lost (draft v329)** (`6a3bf3d`): user screenshot — Step2 ✓ done, Step3 "Transaction failed" + "Fee transaction not yet confirmed". Root cause: the atomic-finalization refactor dropped fee-hash storage on retryFeeClaim's PENDING path — wallet-confirmed fees verified before RPC indexing caught up errored WITHOUT storing the hash → paid fees orphaned, terminal failed UI, Pay Fee would double-charge. Fix: backend PENDING → updateClaimToPendingFee STORES hash + #pendingFee (15s timer verifies/credits); BurnPage treats the message as processing (poller, not terminal); BurnHistory Retry Fee shows in-progress success. Makes Burn History Retry Fee/Retry Claim self-healing for the 10 stuck #pending claims | draft v329, hash `CyHrQcYs` ✓ on draft; canister 6vpy5 upgraded; readbacks ✓ (recipient=collector, armed); 10 test files green; stable-compat ✓ | revert commit |
| — | 09-09 | `index-Bao5vWQi.js` (NEW — frontend UX) | **SINGLE RECOVERY ACTION PER CLAIM (draft v330)** (`b25348a`): user feedback — stuck claims stacked 2-3 actions (PAY FEE + RE-CHECK TX, FEE FAILED + RETRY FEE), users try everything; Recent Burns had NO action. New lib/claim-recovery.ts derives exactly ONE action per state: pending+unverified-burn → Confirming…/Re-check Tx; pending+verified+no-fee → Pay Fee; pending+fee-bound or pendingFee → Confirming fee… (timer; Retry Fee surfaces only on definitive backend fail-marker, no speculative double-pay invite); failed → Retry Claim; verified → none. Burn History renders one status+one action; Recent Burns (BurnPage) now shows the same action inline (Pay Fee/Retry Claim run inline) | draft v330, hash `Bao5vWQi` ✓ on draft; canister 6vpy5 untouched; readbacks ✓ | revert commit |
| — | 09-09 | `index-BDem2hDN.js` (NEW — frontend UX polish) | **BADGE-SIZED ACTION + STATE NAMING (draft v331)** (`706868d`): action button placed directly under the status badge, badge-sized (was full-width PAY FEE bar breaking row layout); status names mirror the agreed mapping with #pending disambiguated via amountBurned — Confirming burn / Confirming fee / GRIT Earned / Burn Failed. **6th backend reset (6vpy5 → ov22c-iaaaa-aaaao-a4mjq-cai)** — and the v331 seed fix WORKED: fresh canister self-configured on first boot (recipient=collector=0x6cBB…aCD2, FeePaid armed, 0.69%) with ZERO manual intervention — the wrong-address class of incident is structurally closed | draft v331, hash `BDem2hDN` ✓ on draft; readbacks on fresh ov22c ✓✓✓✓; getTokens/getAdmins re-verify next session | revert commit |
| — | 09-09 | `index-D1R_5GaY.js` (NEW — frontend + backend) | **BUG-2 + BUG-3 + UX NAMING (draft v332)** (`0197c45`+`cc8b8f0`+`83243da`): ① **Bug-2 relayed-burn BINDING_FAIL** — kVCM retirement burns execute via the embedded-wallet RELAYER (burnTx.from = 0xb42f…, the relayer) so strict sender equality failed every legit relayed kVCM burn. Transfer-log sender (topic[1]) now captured at burn verification and threaded as burnOwner; verifyFeeBinding accepts feeTx.from == burnTx.from OR == burnOwner; new parseBurnOwnerFromReceipt (dead/AAM destination filter) covers paths without a threaded owner — 1 lazy outcall on relayed path only, fail closed. ② **Bug-3 reverted-fee state machine** — recheckFeeClaim TX_FAILED clears the dead hash → #pendingFee-no-fee → Burn History derives Pay Fee (single correct action) instead of endless Confirming fee. ③ **Status naming** — #failed disambiguated via amountBurned: Burn Failed (burn never verified) vs Fee Failed (burn verified, fee stage); BurnPage action button badge-sized under status badge. ④ Latency audit delivered (separate report): ~6 sequential HTTP outcalls mapped, batching + parallel-await recommendations pending user approval | draft v332, hash `D1R_5GaY` ✓ on draft; canister ov22c upgraded in place; readbacks ✓ (recipient=collector, armed); 10 test files green; stable-compat ✓; no .did change | revert commits |
| — | 09-09 | `index-BPqooSJu.js` (NEW — frontend latency) | **LATENCY PACKAGE (draft v333)** (`24926c2`+`80f296b`): ① modal fee-poll 10s→5s; each tick with a #pendingFee claim re-drives retryFeeClaim — if the fee has been indexed since the last attempt, the backend credits IMMEDIATELY instead of waiting the 15s timer (0-15s post-fee tail collapses to ~5s); ② verifyFeeBinding fee+burn tx fetches now run CONCURRENTLY (Motoko bind-then-await) — wall clock 2-6s → 1-3s; ③ JSON-RPC batch experiment REVERTED (hand-rolled Motoko batch parser too fragile — same bug class as the 0xFF sentinel; parallel awaits achieve the same latency win with zero parser risk) | draft v333, hash `BPqooSJu` ✓ on draft; canister ov22c upgraded in place; readbacks ✓ (recipient=collector, armed, 0.69); 10 test files green; stable-compat ✓; no .did change | revert commits |
| — | 09-10 | `index-Ca05AujB.js` (NEW — frontend + backend) | **BADGE LANGUAGE UNIFICATION + FEE-HASH-FIRST (draft v334)** (`0541a48`): user tests found 3-way status mismatches. ① Recent Burns used the OLD ClaimStatusBadge with hardcoded 'Fee Failed — Retry' firing instantly on #pendingFee — contradicted Burn History's 'Confirming fee' for the same claim (axlREGEN/celo showed Fee Failed while backend said Confirming fee → then verified 130.9M grit ✓). Badge unified: Confirming burn / Confirming fee / GRIT Earned / Failed. ② GIV/op: fee PAID + wallet-confirmed, but a DexScreener outage threw inside retryFeeClaim BEFORE the fee-hash store point → paid fee orphaned, claim stuck #pending fee-less, modal showed 'Price unavailable' as Step-3 failure. Fix: retryFeeClaim STORES THE FEE HASH FIRST (before price fetch / verification); on price failure the 15s timer re-runs verification incl. pricing — claim self-heals, no double-pay. ③ Latency report: v333 fixes confirmed live (5s poll + re-drive + parallel fetches); dominant remaining latency = chain confirmation times + intermittent DexScreener failures (W2A/W2B scope) | draft v334 (524 on first attempt — did NOT land; retry clean), hash `Ca05AujB` ✓ on draft; canister ov22c upgraded in place; readbacks ✓ (recipient=collector, armed); 10 test files green; stable-compat ✓; no .did change | revert commits |
| — | 09-10 | `index-Ca05AujB.js` (unchanged — backend-only) | **W3A/W3B: bounded drain + reserve-before-charge + frozen mint intent (draft v335)** (`4146790`): scoped against the ACTUAL architecture — AKK-5/6/7/9 already implemented ~60% of the W3A intent, so the remaining gaps closed without a rewrite: ① drainPendingMints bounded to DRAIN_BATCH_MAX=25 entries/cycle (2 outcalls each, ~150s worst case; queue forms ≤1/block so 25/cycle clears backlog 25× faster than it forms; 500-entry outage backlog clears in ~20 cycles); ② reward cap-clamped (capDecision vs totalAkkMined) BEFORE the fuel-drain loop — a round can never charge fuel for a prize the cap won't pay; near cap the reward is the remaining budget (partial payout, supply lands exactly on 21M); ③ drain never recomputes amount or timestamp — frozen mint intent means retry #1 and #25 send the identical request; ④ newMiningState public helper (tests + future migration Init). W2A skipped per user call (deepest-liquidity pricing regardless of chain is preferred); W2B skipped (allowlist criteria already requires decent liquidity) | draft v335, hash unchanged ✓ (backend-only), canister ov22c upgraded in place; readbacks ✓ (recipient=collector, armed, 0.69); 11 test files green; stable-compat ✓; no .did change | revert commit |
| — | 09-10 | `index-Ca05AujB.js` (unchanged — backend-only) | **SECURITY REVIEW FIXES: F2/F3/F6/F7 + F8/F9 validation (draft v336)** (`02a1cf1` `1a24baf` `0ade463` `b3c23f3` `8810f9e` `60e9252`): adversarial read of the branch, then fixes. ① F3: the 4h staleness bypass on the mining-cycle guard was still live (contrary to the W3A plan) — removed; the guard is strict (its only possible effect was admitting an overlapping cycle, i.e. opening the wrong way). ② F6/F7: reward epoch + miner.lastProcessedBlock now derive from the captured blockId (one source of truth); the drain's dead akkBalances 'un-credit' removed. ③ F2: AKK-7's 'deterministic' created_at_time was NOT stable — the clamp grid shifts across retries (measured: 8715 of 200k blocks = 4.36% straddle the boundary inside the retry burst), so a committed-but-unreported mint could be re-sent under a new hash and minted twice. The first attempt's timestamp is now FROZEN on the queue entry and reused verbatim by every retry + the admin replay (new migration 20260910_200000.mo; check-limit raised to 2 — folding would have broken the input shape of the already-applied feeState step). ④ F9 (found by re-auditing F2): freezing removed the timestamp's self-refresh, leaving only ~2.5 min of margin at the last attempt — any longer delay would make the reward permanently unpayable (#TooOld forever). Added LedgerMint.isFrozenStale + a re-freeze in the drain and the admin replay, applied only at the edge. ⑤ F8: the 25-entry drain bound was arithmetic, not measured — PocketIC run: full drain 0.42s locally (per entry 14.2ms), real mainnet round trips update 1.6-1.8s / query 0.9-2.0s → worst case ~70-150s = 10-22% of the 690s window. Bound is well sized. ⑥ F10: the TRACKED dist/backend.wasm was stale (trapped on fresh install with the pre-W1B 'adminState' message) — refreshed to match the source. Remaining residuals (documented, not fixed): F1 unbounded blockHistory growth, F5 AKK-10 fee check fails open without a native price | draft v336, clean landing (no 5xx), canister ov22c upgraded in place — the migration chain applied on the LIVE draft and all config survived: recipient=collector ✓ armed ✓ 0.69 ✓ allowlist 6 tokens ✓ mint queue depth 0 ✓; 12 test files green; stable-compat ✓; no .did change | revert commits |
| — | 09-09 | `index-DD13Ovfb.js` (unchanged — backend-only) | **EMPTY-FEE-HASH STATE MACHINE FIX (draft v327)** (`d7ed0d1`): user tested v326 claim-first flow — claim appeared in Burn History but the wallet popup never fired (stuck at "confirm fee"). Root cause: initiateClaim with deferred-fee sentinel (feeTxHash='') ran the fee half anyway — RPC on empty hash, claim transitioned to #pendingFee storing ?"" (broken), returned FEE_PENDING which the frontend masks as awaiting-fee. Plus two latent bugs: recheckClaim treated empty fee hash as DEFINITIVE FAILURE (timer killed deferred-fee claims before their fee was paid), and recheckFeeClaim ran RPCs on stored '' every 15s. Fixed: empty fee hash = WAITING state everywhere — initiateClaim verifies burn only + commits priced metadata (#pending, no fee state), recheckClaim skips fee verification (keeps #pending for Pay Fee), recheckFeeClaim guards '' like null. Flow now: burn → claim #pending → wallet popup → fee → retryFeeClaim binds+credits | draft v327, hash unchanged ✓ (backend-only), canister 6vpy5 upgraded (not reset), readbacks: recipient=collector=0x6cbb…aCD2 ✓ armed ✓; 10 test files green; stable-compat ✓; no .did change | revert commit |

| — | 09-11 | `index-Bx_-Pup1.js` (NEW — frontend + backend) | **MINER-CREATION FEE GATE + F1 HARDENING (draft v338)** (`be787bf` plan + `914541b` feature + `a461cac` hardening): `createMiner(name, gritAmount, rate, feeChain, feeTxHash)` — a chain with a configured fee requires a paid FeeCollector tx whose MINE-tagged payload (1+len+4, mutually exclusive with the claim payload) names the caller; amount exact-or-more (0-paid never skips — F1 lesson); single-use via `minerFeeTxs` consumed map (per-chain, W1A-canonical keys); ALL RPC verification precedes one await-free tail (replay → create → consume-on-success-only); migration `20260911_000000` appends `minerFeeTxs` empty; mops.toml check-limit 2→3. Owner decisions: PER-CHAIN gate (fee 0/unset = free; all chains configured when arming), fee wallet = admin.feeRecipient (same FeeCollector as burn fees). Frontend Step 1 sends the MINE payload (was `data:"0x"`), captures hash, maps gate codes to messages. Adversarial review verdict GO: F1 display/enforcement divergence closed by `a461cac` (`setMinerCreationFee` rejects chains with zero allowlisted tokens — MINER_FEE_CHAIN_UNSUPPORTED); F2 unbounded minerFeeTxs growth noted, non-blocking; migration ground truth = `dist/backend.most` (live akk-ledger types were already correct — stale previous-migration inline was the blocker; types/akk-ledger.mo untouched) | draft v338, hash `Bx_-Pup1` ✓ verified on draft URL; canister ov22c upgrade lands on next backend touch (minerFeeTxs migrates empty); 13/13 test files, stable-compat ✓ across 3 pending migrations; Candid changed (createMiner +2 args) — bindgen done | revert commits; Candid revert regenerates bindings |

| — | 09-16 | `index-D29_Kdg4.js` (NEW — frontend + backend) | **v340: REVIEW-PASSED claims fixes + config seeds + GAP-2 (draft 340)** (`5a25ba4` amt==0 recovery credits RECOVERED amount (6fc7a96's version would have marked amt==0 claims #verified with 0 GRIT — caught by review before push) · `8802363` owner config seeds: 1T GRIT/$1 issuance; miner-creation fees ETH/Base/OP 6_900_000_000_000 wei (0.0000069 ETH), Celo 69_000_000_000_000_000 wei (0.069 CELO) — boot-idempotent every boot, runtime-overridable · `67ba8d0` GAP-2: frontend TX_INDEX_RPCS aligned to probed backend endpoint sets + OP fallbacks · `db7bbca` review-found comment drift + probe body truncation). **FIRST KANBAN-DELEGATED INDEPENDENT REVIEW PASSED** — 3 cards on the anti-krisis board (deepseek/deepseek-v4.1-flash via openrouter, read-only, file:line-evidenced): (1) claims-recovery GO — independently confirmed the 6fc7a96 zero-credit defect + 5a25ba4 fix, CAS double-credit proof; (2) timer+seeds GO — seed placement/values/upgrade-re-arm verified, C3 seed-bypasses-F1-allowlist-guard = owner decision before main/live, C4/C5 comment drift fixed in db7bbca; (3) miner-fee gate GO — cross-use both directions / replay keys / tail atomicity / dust all verified safe, D1 gate-is-caller-elected (feeChain=null or misspelled chain skips even on armed chains) = owner decision before main/live, D2 dead plain-transfer fallback comment fixed, D3 relay-tolerance overstated fixed, D6 latent payer-scan asymmetry noted. Gates: mops check ✓, 13/13 test files ✓, build ✓, bindgen ✓ | draft v340, hash `D29_Kdg4` ✓ verified on draft URL; FRESH canister 5wmrk-nyaaa-aaaae-qggla-cai (draft reset) — self-seed verified by readbacks: issuance 1_000_000_000_000 ✓, fees base/celo/optimism/ethereum ✓, recipient=collector=0x6cBB…aCD2 ✓, FeePaid armed ✓ | revert commits; boot seeds are idempotent so rollback is clean |

## Version → commit reconstruction rules


- Caffeine versions increment per successful upload; composer-side pushes may skip numbers.
- Ground truth = draft URL's `index-*.js` hash vs local `pnpm build` output hash.
- v277 = GitHub main `06d5908` (squash-merged export by Caffeine; our commits AKK-2/1b/3/UX
  arrived there as squash commits `55c6221`+`06d5908`).
- Live URL strips `<script>` tags via CDN transform — bundle-hash check ONLY works on the
  draft domain.

## Rollback procedure (tested path)

1. Identify target: live = v277 (`06d5908` on GitHub main).
2. Build locally from that commit, `caffeine preview --build --project . --json` → draft N+1.
3. Verify draft hash, user reviews, promotes draft → live from Caffeine dashboard.
4. GitHub sync happens only after user verifies (their flow).

## After Option B lands, expected entries

- v280: FeeCollector contract + reverted self-send + FeePaid-event check (AKK-4 final form)
- Phase 2: aggregator consolidation + ckETH → ICPSwap → notify_top_up + watchdog
